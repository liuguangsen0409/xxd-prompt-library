# 首页风格看板验证

final result: passed

## 视觉依据与范围

- 参考：`/var/folders/ls/6nnsfcsx0015m98kbldgq2y80000gn/T/codex-clipboard-5cdde029-05b1-4b5f-a12e-86714c0082a2.png`，1660 × 871。
- 桌面最终截图：`/tmp/xxd-board-desktop-final.png`，1660 × 871 CSS 视口；截图为同等像素尺寸，未缩放。
- 聚焦状态：`/tmp/xxd-board-desktop.png`。
- 手机最终截图：`/tmp/xxd-board-mobile.png`，390 × 844 CSS 视口，同等像素尺寸。
- 参考与最终桌面截图在同一次工具输出中打开比较。整体采用参考的三列、三张并排、小圆角和紧凑间距。站点外层导航、最大内容宽度、真实样张及中文工具栏是本次明确采用的项目适配，不按 Midjourney 全站做像素克隆。
- 参考提供的是风格图片；本项目保留现有上下对照样张。逐张精选纯效果图属于后续内容编辑，不声称已完成所有封面的人工策展。

## 迭代记录

1. 初版：默认视口下只有两列；一个 imgBB URL 返回 180 × 180 的服务不可用占位图。修复为 1024px 起三列；请求失败或已识别的图床占位图改用第四张备用样张。
2. 发现 iframe 因小数高度出现内部滚动条。改为内容实际高度向上取整加 1px，并关闭 iframe 自身滚动。手机实测内容 2380px、iframe 2381px；最终桌面截图不再出现内部滚动条。
3. 手机最终截图确认单列、常显名称和简介；DOM 检查内容宽度和 scrollWidth 都为 343px，无横向溢出。
4. 最终参考与实现同屏输入比较，没有待处理的 P0/P1/P2 视觉问题。

## 五项视觉检查

- 字体：沿用站点无衬线字体，标题 16px、简介 12px；层级清晰，简介单行截断避免遮盖过多图片，完整描述仍通过链接的可访问名称提供。
- 布局：三图等宽、卡片约 2:1、12px 间距；桌面三列、手机一列；无嵌套文档导航。
- 色彩：背景与工具栏使用站点 token；说明层使用用户同意的黑色渐变和白字。深色切换后 iframe 背景实测 rgb(9, 9, 11)。
- 图片：使用真实文章样张，没有生成或替换提示词内容。备用图已在原先出错的手绘观察页卡片显示。所有 120 个风格的数据均有三张图片；未逐一验证全部远程 URL 的长期可用性。
- 文案：风格名称与描述来自文章，工具栏和操作为中文；不加入没有数据来源的热门、点赞统计。

聚焦态截图可读到风格名称和简介；此局部已经能直接判定文字层级，不需要额外裁切拼图。

## 功能验证

- API 返回 120 个风格；初批展示 12 个。
- 搜索「复古」得到 6 个结果；不匹配的关键词显示空状态并可清空。
- 加载更多显示 24 / 120；随机排序重新展示 12 个，顺序变化。
- 点击纸艺浮雕志在顶层窗口打开 `/prompts/xxd-panel-014`，详情正常渲染。
- 独立访问 `/embed/styles` 后跳转 `/#styles`。
- 键盘 Tab 聚焦卡片显示名称、简介和箭头；手机宽度常显说明。
- 主题按钮切换后父页和看板均进入深色，再切回原先浅色。
- 最终浏览器 error 日志为空；存在模板已有的页脚图标加载 warning。
- 首页与嵌入页 HTTP 200。
- 全仓库 ESLint 通过。
- Nuxt 类型检查仅剩已有的 DialogScrollContent.vue、useBreadcrumb.ts、useI18nDocs.ts 三处 TS7006，本次新增文件无类型错误。

## 验证限制

未执行完整生产构建或静态生成。已显式配置 iframe 页与数据接口的预渲染路径。触屏设备通过响应式布局与 CSS 检查，未使用真机。
